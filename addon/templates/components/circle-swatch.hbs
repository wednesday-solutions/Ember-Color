<Swatch
  @hexColor={{if isActive "#ffffff" color}}
  @width={{circleSize}}
  @height={{circleSize}}
  @boxShadow={{boxShadow}}
  @className="{{styleNamespace}}__swatch"
  @onClick={{onClick}}
  @onHover={{onHover}} />